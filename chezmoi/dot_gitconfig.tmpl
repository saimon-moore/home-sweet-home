[user]
	name = {{ .name | quote }}
	email = {{ .email | quote }}
{{- if and (eq .target "host") (eq .context "work") }}
	signingkey = ~/.ssh/id_rsa.pub
{{- else if ne .target "host" }}
	signingkey = ~/.ssh/id_ed25519.pub
{{- end }}

[alias]
	ci = commit
	st = status
	co = checkout
	br = branch

[push]
	default = current
	autoSetupRemote = true

[core]
	excludesfile = ~/.gitignore

{{- if and (eq .target "host") (eq .context "work") }}
[commit]
	gpgsign = true

[gpg]
	format = ssh

[gpg "ssh"]
	allowedSignersFile = ~/.ssh/allowed_signers

[credential]
	helper = osxkeychain

{{- else if ne .target "host" }}
[commit]
	gpgsign = true

[gpg]
	format = ssh

[url "git@github.com:"]
	insteadOf = https://github.com/

[url "git@source.xing.com:"]
	insteadOf = https://source.xing.com/

{{- end }}

{{- if and (eq .context "work") (ne .work_username "") }}
[credential "https://source.xing.com"]
	username = {{ .work_username | quote }}
{{- end }}

{{- if ne .github_username "" }}
[github "source.xing.com"]
	user = {{ .github_username | quote }}

[github "api.source.xing.com"]
	user = {{ .github_username | quote }}

[github]
	user = {{ .github_username | quote }}
{{- end }}

[init]
	defaultBranch = main

[pull]
	rebase = false
