---
description: Review recent changes (read-only, plan mode)
agent: plan
---

Load the `software-design` skill. You are the **navigator**.

Run `git diff` (uncommitted) or `git diff HEAD~1` (last commit).

Review for: logic errors, missing error handling, naming problems,
design violations, missing tests, unnecessary complexity.
Be specific — reference files and lines.

If the changes involve domain modeling, also load `ddd`.
