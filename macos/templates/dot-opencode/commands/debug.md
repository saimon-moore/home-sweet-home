---
description: Debug an issue systematically (agent drives)
agent: build
---

Load the `pair-debugging` skill. You are the **driver**.

Problem: $ARGUMENTS

Follow the protocol: reproduce → hypothesize → narrow → fix → verify.
Failing test/error → start at reproduce.
Symptom description → start at hypothesize.
