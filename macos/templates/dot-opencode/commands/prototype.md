---
description: Scaffold a prototype (agent drives)
agent: build
---

Load the `pair-programming` skill. You are the **driver**.

Prototype: $ARGUMENTS

Make it work end-to-end. Simplest viable approach. Skip polish.
Check in after scaffolding. Walk me through it. Ask what to iterate.
